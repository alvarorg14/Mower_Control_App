import 'package:flutter/material.dart';

class MowersScreen extends StatelessWidget {
  const MowersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Mowers')),
    );
  }
}
